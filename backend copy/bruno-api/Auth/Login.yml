info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "email" : "schoolpay41@gmail.com",
        "password" : "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
