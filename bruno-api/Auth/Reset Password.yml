info:
  name: Reset Password
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}api/v1/auth/reset-password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "token": "2947e854-1659-4706-bf9d-42182e89f8b2",
        "password": "admin020905",
        "confirm_password": "admin020905"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
