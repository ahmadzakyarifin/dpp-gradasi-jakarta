info:
  name: " Forgot Password"
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/v1/auth/forgot-password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "schoolpay41@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
